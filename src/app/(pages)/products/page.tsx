import PageHeader from "@/app/ui/components/PageHeader/PageHeader";
import { title } from "process";

const Page = () => {
    return (
        <div>
        <PageHeader title="product" regularButton={{ title: "" }}/>
        </div>
    );
}

export default Page; 